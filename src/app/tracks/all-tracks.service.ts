import { Injectable } from '@angular/core';
import { config } from '../app.config';
import { Track } from '../store/models/party-track.model';

@Injectable({
  providedIn: 'root'
})
export class AllTracksService {

  constructor() { }
}
